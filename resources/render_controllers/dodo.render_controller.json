{
	"format_version": "1.8.0",
	"render_controllers": {
		"controller.render.dodo": {
			"geometry": "Geometry.default",
			"arrays": {
				"textures": {
					"Array.skins": [
						"Texture.variant1",
						"Texture.variant2",
						"Texture.variant3",
						"Texture.variant4"
					]
				}
			},
			"textures": [
				"query.variant < 4 ? Array.skins[query.variant] : query.variant >= 4 && query.variant < 8 ? Texture.halloweenVariant : query.variant >= 8 && query.variant < 12 ? Texture.christmasVariant"
			]
		}
	}
}